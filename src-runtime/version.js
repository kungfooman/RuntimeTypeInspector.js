/**
 * Runtime version, mirrored from the root `package.json` (`runtime-type-inspector`).
 * Keep in sync on release so the Download Log header names the exact build.
 */
const RTI_VERSION = '5.0.3';
export {RTI_VERSION};
