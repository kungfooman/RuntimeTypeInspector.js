/**
 * @param {Map<string, any>} mapping The mapping.
 */
async function loadItems(mapping) {
    return;
}
const map = new Map([
    ["x", 1]
]);
for (const entry of map) {
    console.log("entry", entry);
}
